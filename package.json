{
  "name": "noripot",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "docker compose up -d --build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@types/bun": "latest",
    "drizzle-kit": "^1.0.0-rc.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-orm": "^1.0.0-rc.4"
  }
}
